apiVersion: v1
kind: Service

metadata:
  name: health-service
  namespace: dev

spec:

  type: NodePort

  selector:
    app: health-app

  ports:

    - port: 5000
      targetPort: 5000
      nodePort: 30086