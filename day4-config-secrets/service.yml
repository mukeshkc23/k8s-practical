apiVersion: v1
kind: Service

metadata:
  name: flask-config-service
  namespace: dev

spec:

  type: NodePort

  selector:
    app: flask-config

  ports:

    - port: 5000
      targetPort: 5000
      nodePort: 30083